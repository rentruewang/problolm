name: "Problolm"
description: "Blocks low quality LLM generated PR."

inputs:
  github_token:
    description: "GitHub token"
    required: true
    default: ${{ github.token }}

runs:
  using: "composite"
  steps:
    # Not done yet.
    - run: exit 1;
      shell: bash
